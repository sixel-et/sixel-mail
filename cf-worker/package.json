{
  "name": "sixel-mail-inbound",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker for sixel-mail inbound processing",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.18",
    "wrangler": "^3.0.0"
  }
}
